[project]
name = "arxiv-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Hajra", email = "hajra.khan2108@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=4.0.5",
    "pymupdf>=1.28.2",
    "requests>=2.34.2",
]

[project.scripts]
arxiv-mcp = "arxiv_mcp:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
